## Python

- Use uv for package/commands (e.g., `uv add`, `uv run`).
- Avoid poetry, pip, easy_install.
- Ensure pyproject.toml exists in the repo root; if missing run `uv init`.
- Pin the Python version in pyproject.toml when possible.
